alias: app-generate-image-dalle-mini-jax-py
uid: cbd81fefc52d4d12

automation_alias: script
automation_uid: 5b4e0237da074764

category: Modular ML/AI applications

default_env:
  CM_NUMBER_OF_GENERATED_IMAGES: 1

input_mapping:
  num: CM_NUMBER_OF_GENERATED_IMAGES

deps:
- tags: detect,os
- tags: get,python3
  names:
  - python
  - python3
- tags: get,cuda
  enable_if_env:
    USE_GPU:
    - yes
- tags: get,generic-python-lib,_jax
  skip_if_env:
    USE_GPU:
    - yes
- tags: get,generic-python-lib,_jax_gpu
  enable_if_env:
    USE_GPU:
    - yes

variations:
  gpu:
    env:
      USE_GPU: yes

tags:
- app
- image-classification
- onnx
- python
